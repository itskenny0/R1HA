org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
# Match the Kotlin compile daemon's heap to the Gradle daemon. The default
# 2 GB cap got tight once the dashboards + per-card renderers landed; the
# `:app:compileGithubDebugKotlin` task started OOM-ing during ASM emit for
# the Compose bytecode. 4 GB has plenty of headroom on the supported build
# hosts (CI uses runners with 7 GB+) and the daemon shrinks back when idle.
kotlin.daemon.jvmargs=-Xmx4096m
org.gradle.parallel=true
# Cap concurrent workers at 2 so no more than two flavour variants compile in the
# shared Kotlin daemon at once. With two flavours (github + fdroid) the 4 GB daemon
# had headroom; adding the third (legacy / R1HAL) let three compileKotlin tasks run
# concurrently and the daemon OOM'd mid-IR-lowering (BackendException -> Java heap
# space). Capping at 2 holds peak heap at the proven two-flavour profile (the third
# waits its turn) rather than raising -Xmx, which would risk an OS OOM-kill on the
# smaller CI runners this file targets.
org.gradle.workers.max=2
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# R8 full mode lets the optimiser inline and shrink more aggressively, including
# kotlinx.serialization-generated $$serializer classes that are explicitly kept
# in proguard-rules.pro. Saves ~150-300 KB off the release APK on this codebase.
android.enableR8.fullMode=true

# Kotlin classpath snapshots make incremental builds reuse work from previous
# compilations when only a subset of classes change; meaningful win on local
# dev builds.
kotlin.incremental.useClasspathSnapshot=true

# New resource shrinker is faster and produces smaller APKs by removing unused
# string/drawable references that the legacy shrinker missed.
android.experimental.enableNewResourceShrinker=true
